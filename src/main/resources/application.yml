spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shop
    username: anisazelkia
    password: 
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
